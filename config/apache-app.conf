<Directory /var/www/html>

    # Enables .htaccess to override Apache settings (rewrite rules, etc.)
    AllowOverride All

    # Allows all clients to access this directory
    Require all granted

</Directory>